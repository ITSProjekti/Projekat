@model IEnumerable<ITS_Pravi_Projekat.Models.Osoba>
@{
    ViewBag.Title = "OsobaPrikaz";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2></h2>


<div class="dodaj_dugme">
    <a href='@Url.Action("/Novo/14")'>
        <img id="dodaj" src="@Url.Content("~/Content/Images/add_new_user.png")" alt="edit" />

        <p>DODAJTE NOVU OSOBU</p>
    </a>
</div>
    @using (Html.BeginForm("Create", "Osobe"))
{

    <SCRIPT SRC="~/Scripts/SortTable.js"></SCRIPT>

    <table class="sortable table-hover table table-responsive">
        <thead>
            <tr><th>Ime</th><th></th><th></th><th></th></tr>
        </thead>
        <tbody>
@foreach (var osoba in Model)
{
    <tr><td>@Html.ActionLink(osoba.Ime + " " + osoba.Prezime, "Detaljno", "Osobe", new { id = osoba.ID }, null)
</td>
            <td class="kolona_edit">
                <a class="a_iz_tabele" href='@Url.Action("Izmeni","Osobe", new { id = osoba.ID })'>
                 <img class="editolovka" id="olovka" src="@Url.Content("~/Content/Images/edit.png")" alt="edit" />
                </a> 
             </td>

        <td class="kolona_treca_prazna"> </td>
        <td class="kolona_kanta">
            <a class="a_iz_tabele" href='@Url.Action("ObrisiOsobu","Osobe", new { id = osoba.ID })'>
                <img class="editolovka"id="kanta" src="@Url.Content("~/Content/Images/kanta.png")" alt="edit" />
            </a>
        </td class>

            </tr>
            
}
            
 </tbody>
        
    </table>
    <script type="text/javascript" src="~/Scripts/ChangeImgUrl.js"></script>



      @*  <ol class="list-group">
            @foreach (var osoba in Model)

        {


                <li class="list-group-item">

                    @Html.ActionLink(osoba.Ime + " " + osoba.Prezime, "Izmeni", "Osobe", new { id = osoba.ID }, null)

                    @Html.ActionLink("Novo", "Novo", "Osobe", null, null)

                    @Html.ActionLink("Obrisi", "ObrisiOsobu", "Osobe", new { id = osoba.ID }, null)
                </li>






        }

        </ol>
          *@
        }


   

